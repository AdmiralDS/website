---
import Layout from '../layouts/Layout.astro';
import SubLayout from '../layouts/SubLayout.astro';
import { Header, Content } from '@components/pages/library';
import '@admiral-ds/fonts/VTBGroupUI.css';
---

<Layout title="Библиотека" activeMenuItem="library/#library" currentPage="library">
  <SubLayout>
    <Header slot="header" />
    <Content slot="content" />
  </SubLayout>
</Layout>

<script>
  import { setCurrentPage, setPreviousPage } from '../store/page';

  const url = new URL(window.location);
  if (url.searchParams.has('prev')) {
    const previousPage = url.searchParams.get('prev');
    url.searchParams.delete('prev');

    setPreviousPage(previousPage);
    history.replaceState({}, '', url);
  }

  setCurrentPage('library');
</script>
